# Produção (npm run build / deploy)
# Carregado automaticamente pelo Vite no build
# Token vem APENAS da URL (?token=) — não há fallback de env

VITE_API_URL=https://skinnerapi.selfsolutions.com.br
VITE_PORTAL_RETURN_URL=https://keepweb.selfsolutions.com.br/user-selection
